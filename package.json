{
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:ci": "wsrun build:ci",
    "build": "wsrun build",
    "lint": "wsrun lint",
    "test:ci": "cross-env CI=true wsrun test:ci"
  },
  "prettier": {
    "printWidth": 80,
    "semi": false,
    "trailingComma": "all"
  },
  "devDependencies": {
    "cross-env": "^7.0.2",
    "prettier": "^2.0.5",
    "typescript": "^3.9.7",
    "wsrun": "^5.2.1"
  }
}
